<Application x:Class="McDonaldsPOS.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:McDonaldsPOS.UI.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design -->
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Amber" SecondaryColor="Red" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
            <converters:HexColorToBrushConverter x:Key="HexToBrush"/>
            <converters:CurrencyConverter x:Key="Currency"/>
            <converters:PinToMaskConverter x:Key="PinToMask"/>
            <converters:CountToVisibilityConverter x:Key="CountToVisibility"/>
            <converters:InverseBoolConverter x:Key="InverseBool"/>
            <converters:StringNotEmptyConverter x:Key="StringNotEmpty"/>
            <converters:BoolToSelectionBrushConverter x:Key="BoolToSelectionBrush"/>
            <converters:BoolToStatusBrushConverter x:Key="BoolToStatusBrush"/>

            <!-- McDonald's Brand Colors -->
            <SolidColorBrush x:Key="McDonaldsRed" Color="#DA291C"/>
            <SolidColorBrush x:Key="McDonaldsYellow" Color="#FFC72C"/>
            <SolidColorBrush x:Key="McDonaldsGold" Color="#FFB81C"/>
            <SolidColorBrush x:Key="DarkBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="CardBackground" Color="#2D2D2D"/>
            <SolidColorBrush x:Key="CardBackgroundHover" Color="#3D3D3D"/>

            <!-- Touch-Friendly Button Style -->
            <Style x:Key="TouchButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Height" Value="60"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Padding" Value="16,8"/>
            </Style>

            <!-- Large Touch Button -->
            <Style x:Key="LargeTouchButton" TargetType="Button" BasedOn="{StaticResource TouchButton}">
                <Setter Property="Height" Value="80"/>
                <Setter Property="MinWidth" Value="120"/>
                <Setter Property="FontSize" Value="18"/>
            </Style>

            <!-- Menu Item Card Style -->
            <Style x:Key="MenuItemCard" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Height" Value="100"/>
                <Setter Property="Width" Value="140"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp4"/>
            </Style>

            <!-- Category Button Style -->
            <Style x:Key="CategoryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Height" Value="70"/>
                <Setter Property="MinWidth" Value="110"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <!-- PIN Pad Button Style -->
            <Style x:Key="PinPadButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Height" Value="70"/>
                <Setter Property="Width" Value="70"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Background" Value="{StaticResource CardBackground}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <!-- Order Item Style -->
            <Style x:Key="OrderItemCard" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource CardBackground}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Padding" Value="12"/>
            </Style>

            <!-- Action Button Primary (Red) -->
            <Style x:Key="ActionButtonPrimary" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="Height" Value="56"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Background" Value="{StaticResource McDonaldsRed}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Margin" Value="4"/>
            </Style>

            <!-- Action Button Secondary (Yellow) -->
            <Style x:Key="ActionButtonSecondary" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="Height" Value="56"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Background" Value="{StaticResource McDonaldsYellow}"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Margin" Value="4"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
