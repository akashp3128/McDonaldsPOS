<Window x:Class="McDonaldsPOS.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:McDonaldsPOS.UI.ViewModels"
        xmlns:views="clr-namespace:McDonaldsPOS.UI.Views"
        mc:Ignorable="d"
        Title="McDonald's POS System"
        Height="900" Width="1400"
        MinHeight="768" MinWidth="1024"
        WindowState="Maximized"
        WindowStyle="None"
        Background="{StaticResource DarkBackground}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:POSViewModel}">
            <views:POSView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="#1A1A1A" Height="48" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo/Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <materialDesign:PackIcon Kind="FoodForkDrink" Width="28" Height="28"
                                            Foreground="{StaticResource McDonaldsYellow}"
                                            VerticalAlignment="Center"/>
                    <TextBlock Text="McDonald's POS"
                              FontSize="18" FontWeight="Bold"
                              Foreground="{StaticResource McDonaldsYellow}"
                              VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Current User -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                           Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibility}}"
                           Margin="16,0">
                    <materialDesign:PackIcon Kind="Account" Width="20" Height="20"
                                            Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUserName}"
                              Foreground="White" VerticalAlignment="Center"
                              Margin="8,0,16,0"/>
                    <Button Content="LOGOUT"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Foreground="{StaticResource McDonaldsRed}"
                           Command="{Binding LogoutCommand}"/>
                    <Button Content="KDS"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Foreground="{StaticResource McDonaldsYellow}"
                           Click="OpenKDS_Click"
                           Margin="8,0"/>
                </StackPanel>

                <!-- Window Controls -->
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                           Click="MinimizeButton_Click" Padding="12">
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="20" Height="20"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                           Click="MaximizeButton_Click" Padding="12">
                        <materialDesign:PackIcon Kind="WindowMaximize" Width="20" Height="20"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                           Click="CloseButton_Click" Padding="12"
                           Foreground="{StaticResource McDonaldsRed}">
                        <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
